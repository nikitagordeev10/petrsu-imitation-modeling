//// Функция для вычисления элемента площади поверхности
//Функция ВычислитьЭлементПлощади(u, v)
//    x_u = COS(v);
//    x_v = -u * SIN(v);
//    y_u = SIN(v);
//    y_v = u * COS(v);
//    z_u = 0;
//    z_v = 1;

//    E = x_u * x_u + y_u * y_u + z_u * z_u;
//    F = x_u * x_v + y_u * y_v + z_u * z_v;
//    G = x_v * x_v + y_v * y_v + z_v * z_v;

//    f = sqrt(E * G - F * F);
//    Возврат f;
//КонецФункции

//// Функция для вычисления интеграла методом Монте-Карло
//Функция ВычислитьИнтегралМонтеКарло(num_samples)
//    сумма = 0.0;                   
//	ГСЧ = Новый ГенераторСлучайныхЧисел();
//    Для i = 1 По num_samples Цикл
//        u = (ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0) * 2 * 3.141592653589793;
//        v = (ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0) * 2 * 3.141592653589793;
//        сумма = сумма + ВычислитьЭлементПлощади(u, v);
//    КонецЦикла;
//    Возврат (2 * 3.141592653589793) * (2 * 3.141592653589793) * (сумма / num_samples);
//КонецФункции

//// Функция для генерации случайных точек методом Неймана
//Функция Нейман(f, a, b, M)
//	ГСЧ = Новый ГенераторСлучайныхЧисел();
//	Пока Истина Цикл
//        u1 = ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0;
//        u2 = ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0;
//        x1 = a + (b - a) * u1;
//        x2 = M * u2;
//        Если x2 <= f(x1) Тогда
//            Возврат x1;
//        КонецЕсли;
//    КонецЦикла;
//КонецФункции

//// Основная процедура
//&НаКлиенте
//Процедура Основная()
//    // Параметры программы
//    num_samples = 1000; // Количество сэмплов для Монте-Карло
//    num_points = 50; // Количество точек для генерации

//    // Вычисляем интеграл для нахождения полной площади
//    площадь = ВычислитьИнтегралМонтеКарло(num_samples);
//    Сообщить("Полная площадь поверхности: " + площадь);

//    // Вычисляем максимальное значение f
//    M = 0.0;           
//	ГСЧ = Новый ГенераторСлучайныхЧисел();
//	
//    Для i = 1 По 1000 Цикл
//        u = (ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0) * 2 * 3.141592653589793;
//        v = (ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0) * 2 * 3.141592653589793;
//        f_значение = ВычислитьЭлементПлощади(u, v);
//        Если f_значение > M Тогда
//            M = f_значение;
//        КонецЕсли;
//    КонецЦикла;

//    // Генерация случайных точек
//    точки = Новый Массив(num_points, 3);
//    Для i = 1 По num_points Цикл
//        u = Нейман(ВычислитьЭлементПлощади, 0, 2 * 3.141592653589793, M);
//        v = (ГСЧ.СлучайноеЧисло(0, 1000000) / 1000000.0) * 2 * 3.141592653589793;
//        x = u * COS(v);
//        y = u * SIN(v);
//        z = v;
//		
//	    точка = Новый Массив(3);
//        точка[0] = x;
//        точка[1] = y;
//        точка[2] = z;
//        
//        точки[i - 1] = точка;
//	КонецЦикла;

//    // Вывод точек
//    Для i = 1 По num_points Цикл
//        Сообщить("Точка " + i + ": x = " + точки[i - 1][0] + ", y = " + точки[i - 1][1] + ", z = " + точки[i - 1][2]);
//    КонецЦикла;
//КонецПроцедуры